password = input("Enter password: ")

length_of_password = len(password)

asterisk = "*"
replace_password = (length_of_password * asterisk)
print(replace_password)

#for asterisk in range(length_of_password):
	#print("*", end="")
